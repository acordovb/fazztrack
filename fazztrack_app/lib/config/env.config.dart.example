# Archivo de ejemplo para configuración de entorno
# Copia este archivo como env.config.dart y edita la URL

class EnvConfig {
  // URL del backend - cambia esta URL por la de tu servidor
  static const String baseUrl = 'http://127.0.0.1:3000/api/v1';
}
